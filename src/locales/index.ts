import enDefault from './en/default.json';
import enButton from './en/button.json';
import enError from './en/error.json';
import enStatus from './en/status.json';

import esDefault from './es/default.json';
import esButton from './es/button.json';
import esError from './es/error.json';
import esStatus from './es/status.json';

const resources = {
  en: {
    default: enDefault,
    error: enError,
    button: enButton,
    status: enStatus,
  },
  es: {
    default: esDefault,
    error: esError,
    button: esButton,
    status: esStatus,
  },
};

export default resources;
