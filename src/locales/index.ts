import enDefault from './en/default.json';
import esDefault from './es/default.json';

const resources = {
  en: {
    default: enDefault,
  },
  es: {
    default: esDefault,
  },
};

export default resources;
